/*
# JavaScript 実践練習問題集 (全30問)

これまでの学習内容（変数、条件分岐、ループ、配列、オブジェクト、関数、クラス、モジュール）を網羅した練習問題です。
難易度は徐々に上がっていきます。

---
## Level 1: 基礎 (変数・演算・文字列)
*/

// Q1. 変数と計算
// 半径 `r` が 5 の円の面積を求め、変数 `area` に代入して console.log で表示してください。
// 円周率は 3.14 とします。
const r = 5;
// 👇 ここにコードを書いてください


// Q2. テンプレートリテラル
// 変数 `name` と `age` を使って、"私の名前はKenで、25歳です。" という文字列を
// テンプレートリテラル（バッククォート ``）を使って作成し、console.log で表示してください。
const name = "Ken";
const age = 25;
// 👇 ここにコードを書いてください


// Q3. 真偽値 (Truthy/Falsy)
// 変数 `val` が "Falsy" な値（false, 0, "", null, undefined, NaN）であれば "NG"、
// そうでなければ "OK" と表示するコードを書いてください。
const val = 0;
// 👇 ここにコードを書いてください


/*
---
## Level 2: 条件分岐とループ
*/

// Q4. 条件分岐 (if)
// 変数 `score` が 80以上なら "合格"、それ以外なら "不合格" と表示するコードを書いてください。
const score = 75;
// 👇 ここにコードを書いてください


// Q5. 条件分岐 (switch)
// 変数 `rank` が "A" なら "素晴らしい"、"B" なら "良い"、"C" なら "頑張ろう"、
// それ以外なら "不明" と表示する switch 文を書いてください。
const rank = "B";
// 👇 ここにコードを書いてください


// Q6. ループ (for)
// 1 から 10 までの数字を順番に console.log で表示してください。
// 👇 ここにコードを書いてください


// Q7. ループ (while)
// 変数 `count` が 0 になるまで、1ずつ減らしながら console.log で表示する while ループを書いてください。
let count = 5;
// 👇 ここにコードを書いてください


/*
---
## Level 3: 関数とスコープ
*/

// Q8. 通常の関数
// 2つの数字を引数として受け取り、大きい方の数字を返す関数 `getMax` を作成してください。
// 👇 ここにコードを書いてください


// Q9. アロー関数
// 文字列を引数として受け取り、その文字数が 5文字以上なら true、そうでなければ false を返す
// アロー関数 `isLongString` を作成してください（{} と return を省略した書き方で）。
// 👇 ここにコードを書いてください


// Q10. デフォルト引数
// 名前を受け取って "Hello, 〇〇!" と返す関数 `sayHello` を作成してください。
// もし名前が渡されなかった場合は "Hello, Guest!" となるようにしてください。
// 👇 ここにコードを書いてください


// Q11. スコープ
// 以下のコードはエラーになります。関数の中で定義された変数 `message` を
// 関数の外で使えるように修正（returnを使うなど）し、console.logで表示できるようにしてください。
/*
function createMessage() {
    const message = "Secret";
}
createMessage();
console.log(message); // エラー！
*/
// 👇 ここに修正したコードを書いてください


/*
---
## Level 4: 配列とオブジェクトの基礎
*/

// Q12. 配列の操作
// 配列 `colors` の最後に "green" を追加し、最初の要素を削除してください。
const colors = ["red", "blue", "yellow"];
// 👇 ここにコードを書いてください


// Q13. 配列のループ (forEach)
// 配列 `numbers` のすべての要素を2倍にした値を console.log で表示してください。
const numbers = [10, 20, 30];
// 👇 ここにコードを書いてください


// Q14. オブジェクトの参照
// 下記の `user` オブジェクトから、"Tokyo" という文字列を取り出して表示してください。
const user = {
    userName: "Ken",
    address: {
        city: "Tokyo",
        zip: "100-0001"
    }
};
// 👇 ここにコードを書いてください


// Q15. オブジェクトの配列
// 次の `products` 配列の中で、価格(price)が 1000 以上の商品の名前だけを console.log で表示してください（for文を使用）。
const products = [
    { productName: "消しゴム", price: 100 },
    { productName: "ノート", price: 200 },
    { productName: "万年筆", price: 1200 },
    { productName: "手帳", price: 1500 },
];
// 👇 ここにコードを書いてください


// Q16. undefinedのチェック
// `person` オブジェクトに `age` プロパティが存在すればその値を、
// 存在しなければ "年齢不詳" と表示するコードを書いてください（if文 または 論理演算子 || を使用）。
const person = { name: "Taro" };
// 👇 ここにコードを書いてください


/*
---
## Level 5: 配列メソッドの応用 (map, filter, find)
*/

// Q17. filter
// 以下の配列から、偶数（2で割り切れる数）だけを集めた新しい配列 `evenNumbers` を作ってください。
const nums = [1, 2, 3, 4, 5, 6];
// 👇 ここにコードを書いてください


// Q18. map
// 以下の配列の要素をすべて大文字に変換した新しい配列 `upperNames` を作ってください。
const names = ["alice", "bob", "charlie"];
// 👇 ここにコードを書いてください


// Q19. find
// 以下の配列から、id が 3 のユーザーオブジェクトを見つけて `target` 変数に入れてください。
const usersData = [
    { id: 1, name: "A" },
    { id: 2, name: "B" },
    { id: 3, name: "C" },
];
// 👇 ここにコードを書いてください


// Q20. メソッドチェーン
// 以下の配列から、「価格が 500円以下」の商品の「名前だけ」を集めた配列を作ってください。
// filter と map をつなげて書いてみましょう。
const items = [
    { name: "Apple", price: 100 },
    { name: "Banana", price: 200 },
    { name: "Cherry", price: 1000 },
];
// 👇 ここにコードを書いてください


/*
---
## Level 6: 実践的なロジック
*/

// Q21. 論理演算子の活用
// 変数 `a` と `b` が両方とも 10以上なら "Excellent"、
// どちらか片方でも 10以上なら "Good"、それ以外は "Bad" と表示してください。
const a = 12;
const b = 8;
// 👇 ここにコードを書いてください


// Q22. オブジェクトの更新
// `settings` オブジェクトの `theme` を "light" に変更し、
// さらに新しいプロパティ `isAdmin` (値は true) を追加して、console.log で表示してください。
const settings = {
    theme: "dark",
    notifications: true,
};
// 👇 ここにコードを書いてください


// Q23. 配列の検索 (includes)
// 配列 `fruits` の中に "banana" が含まれているかどうかを調べ、
// 含まれていれば "あります"、なければ "ありません" と表示してください。
const fruits = ["apple", "grape", "orange"];
// 👇 ここにコードを書いてください


// Q24. 入力チェック (バリデーション)
// 変数 `inputScore` が 0以上、かつ 100以下 なら "有効な点数です"、
// そうでなければ "無効な点数です" と表示してください。
const inputScore = 105;
// 👇 ここにコードを書いてください


// Q25. データの集計 (reduceの代わり)
// カートの中身の合計金額を計算してください。配列メソッド(forEachなど)を使っても、for文でも構いません。
const cart = [
    { item: "Book", price: 1500 },
    { item: "Pen", price: 200 },
    { item: "Note", price: 300 },
];
// 👇 ここにコードを書いてください


/*
---
## Level 7: クラスとモジュール (少しだけ)
*/

// Q26. クラスの定義
// `Animal` クラスを作成してください。
// コンストラクタで `name` を受け取り、`speak()` メソッドで "〇〇が鳴いています" と表示します。
// 👇 ここにコードを書いてください


// Q27. 継承
// `Animal` クラスを継承した `Dog` クラスを作成してください。
// `speak()` メソッドをオーバーライドして "〇〇: ワンワン！" と表示するようにしてください。
// 👇 ここにコードを書いてください


// Q28. モジュールのエクスポート
// 以下の関数 `multiply` を、他のファイルで使えるように「名前付きエクスポート」する書き方に変えてください。
// const multiply = (a, b) => a * b;
// 👇 ここにコードを書いてください


/*
---
## Level 8: 総合課題
*/

// Q29. コールバック関数
// 引数として「回数(n)」と「実行したい関数(action)」を受け取る関数 `repeat` を作ってください。
// `action` を n回 実行します。
// 👇 ここにコードを書いてください


// Q30. Todoリストの管理クラス
// 以下の機能を持つ `TodoList` クラスを作成してください。
// 1. プロパティ: `todos` (配列)
// 2. メソッド: `add(task)` -> タスクを配列に追加
// 3. メソッド: `show()` -> 現在のタスク一覧を表示
// 👇 ここにコードを書いてください
