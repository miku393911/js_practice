# JavaScript 実践練習問題 Part 1

今回アップロードされた画像の内容（繰り返し処理、配列の基礎、条件分岐）は、プログラミングの「ロジック（論理）」を組み立てるための最も重要な基礎です。
ここが弱いと、複雑なアプリを作るときに手が止まってしまいます。

## Level 1: 定番のロジック練習 (FizzBuzz)

プログラマの採用試験でもよく出る、条件分岐とループの定番問題です。

### 要件
1. `fizzBuzz` 関数を作成してください。
2. `1` から `100` までの数字を順番に `console.log` で出力してください。
3. ただし、以下のルールに従ってください：
    *   3の倍数のときは数字の代わりに `"Fizz"` と出力
    *   5の倍数のときは数字の代わりに `"Buzz"` と出力
    *   3と5の両方の倍数（15など）のときは `"FizzBuzz"` と出力
    *   それ以外はそのまま数字を出力

```javascript
const fizzBuzz = () => {
  // 👇 ここに for文 と if文 を使って書いてください
  
};

fizzBuzz();
/* 出力例:
1
2
Fizz
4
Buzz
Fizz
...
14
FizzBuzz
...
*/
```

---

## Level 2: 最大値を探せ (配列とループ)

便利なメソッド（`Math.max`など）を使わずに、ループ処理だけで配列の中の最大値を見つける練習です。
「変数の値を更新していく」という感覚を養います。

### 要件
1. `findMax` 関数を作成してください。
2. 引数として数値の配列 `numbers` を受け取ります。
3. 配列の中身を `for` 文（または `for...of`）で順番に見ていき、一番大きい数字を見つけて `return` してください。

```javascript
const findMax = (numbers) => {
  let maxVal = numbers[0]; // 最初の要素を仮の最大値とする

  // 👇 ここにループ処理を書いて、maxVal より大きい数字があったら maxVal を更新する処理を書いてください


  return maxVal;
};

const scores = [10, 50, 80, 20, 95, 30];
console.log(`最高得点: ${findMax(scores)}点`); // 出力: 95点
```

---

## Level 3: ステータス判定 (switch文)

API通信などでよくある「ステータスコード」に応じたメッセージの出し分けです。
条件がたくさんある場合は `if` よりも `switch` が見やすいことがあります。

### 要件
1. `getStatusMessage` 関数を作成してください。
2. 引数 `status` (文字列) を受け取ります。
3. `switch` 文を使って、以下の文字列を返してください。
    *   `"success"` -> `"成功しました！"`
    *   `"error"` -> `"エラーが発生しました"`
    *   `"loading"` -> `"読み込み中..."`
    *   それ以外 -> `"不明なステータスです"`

```javascript
const getStatusMessage = (status) => {
  // 👇 ここに switch文 を書いてください

};

console.log(getStatusMessage("success")); // 成功しました！
console.log(getStatusMessage("loading")); // 読み込み中...
console.log(getStatusMessage("unknown")); // 不明なステータスです
```
# JavaScript 実践練習問題 02

Progateで学んだ内容（変数、関数、オブジェクト、クラス、継承など）を使って、実際のアプリ開発でよくあるシチュエーションを想定した問題を3つ作成しました。

## Level 1: ショッピングカートの集計（配列・オブジェクト・関数）

ECサイトで、カートに入っている商品の合計金額を計算する機能を作ります。

### 要件
1. `calculateTotal` 関数を完成させてください。
2. 合計金額が **5000円以上** なら「送料」は無料（0円）、それ未満なら送料500円を加算してください。
3. 最終的な支払い金額を `return` してください。

```javascript
const cartItems = [
  { name: "Tシャツ", price: 2000, count: 2 },
  { name: "キャップ", price: 1500, count: 1 },
];

const calculateTotal = (items) => {
  // 👇 ここに処理を書いてください
  
};

console.log(`支払い合計: ${calculateTotal(cartItems)}円`);
```

---

## Level 2: ユーザー管理システム（クラス・メソッド）

SNSのようなアプリで、ユーザー情報を管理するクラスを作ります。

### 要件
1. `User` クラスを定義してください。
2. コンストラクタで `name`（名前）と `age`（年齢）を受け取り、プロパティに設定してください。
3. `greet` メソッドを作り、`"こんにちは、私は〇〇です。"` と出力してください。
4. `isAdult` メソッドを作り、20歳以上なら `true`、未満なら `false` を返してください。

```javascript
class User {
  // 👇 ここにクラスの中身を書いてください

}

const user1 = new User("田中", 25);
const user2 = new User("佐藤", 18);

user1.greet(); // 出力: こんにちは、私は田中です。
console.log(user1.isAdult()); // 出力: true
console.log(user2.isAdult()); // 出力: false
```

---

## Level 3: RPGのキャラクター（継承・オーバーライド）

ゲームのキャラクターを管理します。勇者は特別な攻撃ができます。

### 要件
1. `Character` クラス（親）を作成します。
    *   プロパティ: `name`, `hp`
    *   メソッド: `attack()` -> `〇〇の攻撃！` と出力
2. `Hero` クラス（子）を `Character` から継承して作成します。
3. `Hero` クラスに `superAttack()` メソッドを追加します。
    *   処理: `〇〇の超必殺技！100のダメージ！` と出力し、自身の `hp` を10減らす。
4. `Hero` クラスで `attack()` をオーバーライド（上書き）します。
    *   処理: `〇〇は勇者の剣で攻撃した！` と出力。

```javascript
class Character {
  constructor(name, hp) {
    this.name = name;
    this.hp = hp;
  }
  
  attack() {
    console.log(`${this.name}の攻撃！`);
  }
}

// 👇 Heroクラスをここに書いてください



// 実行テスト
const hero = new Hero("勇者", 200);
hero.attack();       // 出力: 勇者は勇者の剣で攻撃した！
hero.superAttack();  // 出力: 勇者の超必殺技！100のダメージ！
console.log(hero.hp); // 出力: 190
```

# JavaScript 実践練習問題 Part 3


追加でアップロードされた画像の内容（配列メソッド、コールバック関数、ファイルの分割）に焦点を当てた、より実践的な問題集です。
特に `map`, `filter`, `find` や `import/export` は、Reactなどのモダンな開発で**呼吸をするように使う**技術です。

## Level 1: 配列操作マスター（map, filter, find）

大量のデータから必要な情報を抜き出す処理です。

### 要件
1. `activeUsers` 変数を作成し、`users` 配列から `isActive` が `true` のユーザーだけを **filter** で抽出してください。
2. `userNames` 変数を作成し、`activeUsers` から名前（`name`）だけの配列を **map** で作成してください。
3. `targetUser` 変数を作成し、`users` 配列から `id` が `3` のユーザーを **find** で探してください。

```javascript
const users = [
  { id: 1, name: "佐藤", age: 25, isActive: true },
  { id: 2, name: "鈴木", age: 19, isActive: false },
  { id: 3, name: "高橋", age: 30, isActive: true },
  { id: 4, name: "田中", age: 22, isActive: false },
];

// 1. アクティブなユーザーだけを抽出 (filter)
const activeUsers = // 👇 ここに書いてください

// 2. アクティブなユーザーの名前だけの配列を作る (map)
const userNames = // 👇 ここに書いてください

// 3. IDが3のユーザーを探す (find)
const targetUser = // 👇 ここに書いてください

console.log(activeUsers); // 佐藤さんと高橋さんのオブジェクトが表示される
console.log(userNames);   // ["佐藤", "高橋"]
console.log(targetUser);  // 高橋さんのオブジェクト
```

---

## Level 2: コールバック関数の理解

「関数を引数として渡す」という概念の練習です。

### 要件
1. `processNumbers` 関数を定義してください。
    *   引数: `numbers`（数値の配列）, `callback`（関数）
    *   処理: 配列のすべての要素に対して `callback` 関数を実行し、その結果を新しい配列として返してください（自作の `map` みたいなものを作ります）。
2. `processNumbers` を呼び出すとき、**「数値を2倍にする処理」** をコールバックとして渡してください。

```javascript
// 1. 関数を定義
const processNumbers = (numbers, callback) => {
  const results = [];
  // 👇 ここで forEach などを使い、callbackを実行して results に push してください

  return results;
};

const data = [1, 2, 3, 4];

// 2. 呼び出し (アロー関数で「2倍にする処理」を渡す)
const doubled = processNumbers(data, (num) => {
  // 👇 ここに処理を書く
});

console.log(doubled); // [2, 4, 6, 8]
```

---

## Level 3: ファイルの分割（import / export）

実際の開発ではファイルが分かれます。その書き方の練習です。

### 要件
1. `mathUtils.js` というファイルがあると仮定します。そこで `add` 関数（足し算）を定義し、**名前付きエクスポート (Named Export)** してください。
2. `main.js` というファイルで、上記をインポートして使ってください。

```javascript
// --- mathUtils.js ---

// 👇 add関数を定義し、exportしてください
const add = (a, b) => a + b;



// --- main.js ---

// 👇 ここで import してください


console.log(add(5, 3)); // 8
```
